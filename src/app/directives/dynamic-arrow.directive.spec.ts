import { DynamicArrowDirective } from './dynamic-arrow.directive';

describe('DynamicArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicArrowDirective();
    expect(directive).toBeTruthy();
  });
});
